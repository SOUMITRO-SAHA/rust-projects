# Automatically Generates Documentation

```rs
/// A Color Enumeration
enum Color {
    Red, Green, Blue
}

/// A piece of mail.
struct Mail {
    address: String,
}

/// Adds two numbers together.
fn add(a:i32, b:i32) -> i32 {
    a + b
}
```

## Accessing the Rust Documentation via command

```sh
rustup doc
```
